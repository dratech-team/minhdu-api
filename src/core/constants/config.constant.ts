export enum NODE_ENV {
  DEVELOPMENT = "development",
  PRODUCTION = "production",
  SUB_PRODUCTION = "sub-production",
  STAGING = "staging",
  LOCAL = "local",
}

export const NODE_ENV_LIST = Object.values(NODE_ENV);
